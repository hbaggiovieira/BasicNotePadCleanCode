<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BasicNotePad" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_3</item>
        <item name="colorPrimaryVariant">@color/brand_2</item>
        <item name="colorOnPrimary">@color/brand_3</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/brand_2</item>
        <item name="colorSecondaryVariant">@color/brand_3</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.BasicNotePad.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">#85584a</item>
        <item name="android:windowBackground">@color/brand_2</item>
    </style>

    <style name="Theme.BasicNotePad.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BasicNotePad.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>